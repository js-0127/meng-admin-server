export default () => ({
    minio: {
            isGlobal: true,
            endPoint: 'localhost',
            port: 9000,
            useSSL: false,
            accessKey: 'minio',
            secretKey: 'minio@123',
          }
    
})